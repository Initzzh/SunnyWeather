<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/weatherLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:scrollbars="none"
    android:overScrollMode="never"
    android:visibility="invisible">
    <!--android:overScrollMode="never" 禁止过度滚动的效果（拉伸或发光效果）-->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >
        <include layout="@layout/now"
            android:id="@+id/includeNow"/>
        <include layout="@layout/forecast"
            android:id="@+id/includeForecast"
            />
        <include layout="@layout/life_index"
            android:id="@+id/includeLifeIndex"
            />

    </LinearLayout>
    <!--<androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <include layout="@layout/now"
            android:id="@+id/includeNow"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/includeForecast"
            />

        <include layout="@layout/forecast"
            android:id="@+id/includeForecast"
            app:layout_constraintTop_toBottomOf="@+id/includeNow"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/includeLifeIndex"

            />

        <include layout="@layout/life_index"
            android:id="@+id/includeLifeIndex"
            app:layout_constraintTop_toBottomOf="@+id/includeForecast"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>-->


</ScrollView>